# Curated development-oracle notes

The consumed original and curated development copies normalize wall-mount work to the established HustleXP `assembly` taxonomy. Historical result files remain unchanged as records of the configurations and oracles used when they were produced:

- All 14 wall-mounted mirror variants are `assembly`: `blind2_0204`, `0209`, `0214`, `0219`, `0224`, `0229`, `0234`, `0239`, and `0557` through `0562`.
- All six wall-mounting-an-already-assembled-bookshelf variants are `assembly`: `blind2_0611` through `blind2_0616`.

This follows existing repository policy: the assembly questionnaire owns `wall_mounting`, focused intake regressions treat wall mounting as assembly, and classifier training includes wall mounting under assembly. Towel-bar/rack installation remains `handyman`; the damaged-wall inspection oracle remains `home_services`.

