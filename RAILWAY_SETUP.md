# ⚠️ RAILWAY PRODUCTION SETUP - DO NOT COMMIT THIS FILE ⚠️

These keys were redacted from the repository to fix the GitHub Secret Scanning block.
You MUST manually add these to your Railway Service -> Variables tab.

## 1. Authentication (Firebase)
`FIREBASE_PRIVATE_KEY`
```
[REDACTED PRIVATE KEY]
```

`FIREBASE_PROJECT_ID`
```
hustlexp-fly-new
```
`FIREBASE_CLIENT_EMAIL`
```
firebase-adminsdk-fbsvc@hustlexp-fly-new.iam.gserviceaccount.com
```

## 2. API Keys (AI)
`OPENAI_API_KEY`
```
[REDACTED]
```

`DEEPSEEK_API_KEY`
```
[REDACTED]
```

`GROQ_API_KEY`
```
[REDACTED]
```

## 3. Communication (Twilio / SendGrid)
`TWILIO_ACCOUNT_SID`
```
[REDACTED]
```
`TWILIO_AUTH_TOKEN`
```
[REDACTED]
```
`TWILIO_VERIFY_SERVICE_SID`
```
[REDACTED]
```
`SENDGRID_API_KEY`
```
[REDACTED]
```

## 4. Storage (R2)
`R2_ACCESS_KEY_ID`
```
[REDACTED]
```
`R2_SECRET_ACCESS_KEY`
```
[REDACTED]
```
`R2_ACCOUNT_ID`
```
[REDACTED]
```
`R2_BUCKET_NAME`
```
hustlexpbucket
```

## 5. Payments (Stripe)
`STRIPE_SECRET_KEY`
```
[REDACTED]
```
`STRIPE_WEBHOOK_SECRET`
```
[REDACTED]
```

## 6. Database / Redis
`DATABASE_URL`
```
[REDACTED]
```
`UPSTASH_REDIS_REST_URL`
```
[REDACTED]
```
`UPSTASH_REDIS_REST_TOKEN`
```
[REDACTED]
```
