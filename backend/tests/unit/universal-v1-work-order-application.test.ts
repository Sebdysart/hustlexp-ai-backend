import { describe, expect, it, vi } from 'vitest';
import { UniversalV1WorkOrderApplication } from '../../src/services/UniversalV1WorkOrderApplication.js';
import { transactionBoundDatabase } from '../../src/services/UniversalV1WorkOrderPostgresRepository.js';

const ids={task:'10000000-0000-4000-8000-000000000001',draft:'10000000-0000-4000-8000-000000000002',scope:'10000000-0000-4000-8000-000000000003',route:'10000000-0000-4000-8000-000000000004',provider:'10000000-0000-4000-8000-000000000005',poster:'10000000-0000-4000-8000-000000000006',prior:'10000000-0000-4000-8000-000000000007',interest:'10000000-0000-4000-8000-000000000008',elig:'10000000-0000-4000-8000-000000000009',hold:'10000000-0000-4000-8000-000000000010',estimate:'10000000-0000-4000-8000-000000000011'};
const base={task_id:ids.task,task_draft_id:ids.draft,scope_version_id:ids.scope,scope_version:1,routing_decision_id:ids.route,provider_user_id:ids.provider,provider_organization_id:null,provider_class:'GENERAL_SERVICE_PROVIDER' as const,trade_credential_id:null,predecessor_eligibility_id:ids.prior,predecessor_eligibility_version:1,predecessor_valid_until:new Date(Date.now()+60_000).toISOString()};
describe('UniversalV1WorkOrderApplication',()=>{
 it('reuses the outer QueryFn without opening a nested transaction',async()=>{const query=vi.fn().mockResolvedValue({rows:[],rowCount:0});const outer:any={transaction:vi.fn(),serializableTransaction:vi.fn(),query:vi.fn(),readQuery:vi.fn()};const bound=transactionBoundDatabase(query,outer);await bound.transaction(async q=>q('SELECT 1'));await bound.serializableTransaction(async q=>q('SELECT 2'));expect(query).toHaveBeenCalledTimes(2);expect(outer.transaction).not.toHaveBeenCalled();expect(outer.serializableTransaction).not.toHaveBeenCalled();});
 it('derives interest authority and rejects stale client scope',async()=>{const facts:any={interest:vi.fn().mockResolvedValue(base)};const repo:any={express:vi.fn().mockResolvedValue({replayed:false})};const app=new UniversalV1WorkOrderApplication(facts,repo,vi.fn() as any);await expect(app.expressProviderInterest(ids.provider,{task_id:ids.task,expected_scope_version:2,idempotency_key:'interest:test:0001',client_ts:new Date().toISOString()})).rejects.toMatchObject({code:'WORK_ORDER_VERSION_CONFLICT'});expect(repo.express).not.toHaveBeenCalled();});
 it('authorizes fake finance before delegating the one-transaction command',async()=>{const hold={...base,poster_user_id:ids.poster,interest_application_id:ids.interest,eligibility_decision_id:ids.elig,eligibility_version:2,eligibility_valid_until:new Date(Date.now()+60_000).toISOString(),conditional_hold_id:ids.hold,hold_reserved_at:new Date().toISOString(),hold_expires_at:new Date(Date.now()+60_000).toISOString(),provider_estimate_submission_id:ids.estimate,customer_total_cents:12000,currency:'USD'};const facts:any={workOrder:vi.fn().mockResolvedValue(hold)};const financeFor=vi.fn();const authorize=vi.fn().mockReturnValue(financeFor);const repo:any={materialize:vi.fn().mockImplementation(async(_c:any,_key:string,_actor:string,builder:any)=>{expect(authorize).toHaveBeenCalledOnce();expect(builder).toBe(financeFor);return {work_order_id:'x',hard_assignment_created:false,payment_creation_performed:false};})};const app=new UniversalV1WorkOrderApplication(facts,repo,authorize);const out=await app.secureAndMaterializeFakeWorkOrder(ids.poster,{conditional_hold_id:ids.hold,expected_eligibility_version:2,idempotency_key:'workorder:test:0001',client_ts:new Date().toISOString()});expect(repo.materialize).toHaveBeenCalledWith(hold,'workorder:test:0001',ids.poster,financeFor);expect(out).toMatchObject({hard_assignment_created:false,payment_creation_performed:false});});
});
