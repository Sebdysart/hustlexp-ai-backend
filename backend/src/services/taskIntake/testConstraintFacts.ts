import assert from 'node:assert/strict';
import { extractConstraintFacts, type ConstraintFactType } from './extractConstraintFacts.js';
type ExpectedFact={type:ConstraintFactType;value:boolean|number};
const CASES=[{input:'Move a refrigerator down one flight of stairs.',expected:[{type:'stair_flights',value:1},{type:'stairs',value:true}]},{input:'Move some furniture from upstairs; narrow staircase.',expected:[{type:'stairs',value:true},{type:'narrow_access',value:true}]},{input:'Pick up a washing machine and bring it upstairs.',expected:[{type:'stairs',value:true}]},{input:'Move a sofa up five flights of stairs with no elevator.',expected:[{type:'stair_flights',value:5},{type:'stairs',value:true},{type:'no_elevator',value:true}]},{input:'Move furniture but the hallway is only 30 inches wide.',expected:[{type:'narrow_access',value:true}]},{input:'Need handyman work in a rental property; landlord approval may be required.',expected:[{type:'approval_required',value:true}]},{input:'Clean a house but the water is currently shut off.',expected:[{type:'water_unavailable',value:true}]},{input:'Yard cleanup, but there is no outdoor power outlet.',expected:[{type:'power_unavailable',value:true}]},{input:'Car will not start and it is parked in an underground garage.',expected:[{type:'underground_garage',value:true}]},{input:'Moving next week, second floor now, new place has elevator.',expected:[{type:'floor_access',value:2},{type:'elevator_available',value:true}]}];
const failures:string[]=[];for(const c of CASES){const actual=extractConstraintFacts(c.input).map(f=>({type:f.type,value:f.value}));for(const expected of c.expected){try{assert.equal(actual.some(a=>a.type===expected.type&&a.value===expected.value),true);}catch{failures.push([c.input,`missing=${JSON.stringify(expected)}`,`actual=${JSON.stringify(actual)}`].join(' | '));}}}if(failures.length){console.error(`Constraint fact failures: ${failures.length}/${CASES.length}`);for(const f of failures)console.error(`- ${f}`);process.exitCode=1;}else console.log(`Constraint fact cases passed: ${CASES.length}/${CASES.length}`);

