import type { IntakeAnswers, TaskCategory } from './types.js';
const str=(a:IntakeAnswers,k:string)=>typeof a[k]==='string'&&a[k].trim()?a[k] as string:null;
const num=(a:IntakeAnswers,k:string)=>typeof a[k]==='number'&&Number.isFinite(a[k])?a[k] as number:null;
const bool=(a:IntakeAnswers,k:string)=>typeof a[k]==='boolean'?a[k] as boolean:null;
const list=(a:IntakeAnswers,k:string)=>Array.isArray(a[k])?a[k] as string[]:[];
const pretty=(v:string)=>v.replace(/_/g,' ').replace(/\b\w/g,c=>c.toUpperCase());
const sentence=(v:string)=>/[.!?]$/.test(v.trim())?v.trim():`${v.trim()}.`;
export function buildTaskScopeSummary(category:TaskCategory,rawInput:string,a:IntakeAnswers):string{const p:string[]=[];switch(category){case'yard':{const s=str(a,'yard_size');if(s)p.push(`${pretty(s)} yard or outdoor area.`);const d=bool(a,'debris');if(d===true)p.push(list(a,'debris_type').length?`Debris haul-away is required for ${list(a,'debris_type').map(pretty).join(', ')}.`:'Debris haul-away is required.');else if(d===false)p.push('Debris haul-away is not required.');const e=bool(a,'equipment_provided');if(e===true)p.push('Customer will provide the required yard equipment.');if(e===false)p.push('Provider must bring the required yard equipment.');break}case'cleaning':{const property=str(a,'property_type'),rooms=num(a,'room_count'),kind=str(a,'cleaning_type');if(property||rooms!==null)p.push(`${kind?pretty(kind):'Cleaning'} for ${property?`a ${pretty(property).toLowerCase()}`:'the property'}${rooms!==null?` covering approximately ${rooms} room${rooms===1?'':'s'}`:''}.`);const supplies=bool(a,'supplies_provided');if(supplies===true)p.push('Customer will provide cleaning supplies and equipment.');if(supplies===false)p.push('Provider should bring cleaning supplies and equipment.');break}case'moving':{const n=num(a,'item_count');if(n!==null)p.push(`Approximately ${n} item${n===1?'':'s'} need to be moved.`);const stairs=bool(a,'stairs');if(stairs===true){const f=num(a,'stair_flights');p.push(f!==null?`${f} flight${f===1?'':'s'} of stairs are involved.`:'Stairs are involved.')}else if(stairs===false)p.push('No stairs are involved.');if(bool(a,'large_items')===true){const d=str(a,'large_item_details');p.push(d?`Large or heavy items: ${sentence(d)}`:'Large or heavy items are involved.')}if(bool(a,'vehicle_required')===true)p.push('Provider must supply transportation.');break}default:{for(const k of ['task_goal','work_type','service_type','auto_service_type','event_type','pet_type','delivery_item','assembly_type']){const v=str(a,k);if(v)p.push(sentence(v));}break}}const access=str(a,'access_restrictions'),constraints=str(a,'special_constraints');if(access)p.push(`Access notes: ${sentence(access)}`);if(constraints)p.push(`Additional constraints: ${sentence(constraints)}`);return p.length?p.join(' '):rawInput.trim()}
