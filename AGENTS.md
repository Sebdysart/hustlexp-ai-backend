# HustleXP engineering agent entry point

Scope: backend repository. Documentation handoff candidate dated 2026-09-21; adoption/release are separate. This file creates no deployment, payment, signing, migration or sharing permission.

## Start a session

Read README.md, docs/HUSTLEXP_CURRENT_BACKEND_CHECKPOINT.md, the applicable existing repository safety/CLAUDE instructions, and the current Canonical Index linked there. Read the latest relevant Shared Goal, Execution & Review Log entry and accessible local handoff; record exact code pair, local delta, environment identity and evidence access. Do not turn an old named candidate into current authority or restore old hashes over newer work.

The business is a managed local-work transaction network, not a mandate to rebuild historical gamification. The current Google Charter controls product intent; code/runtime describes actual behavior; executed external decisions constrain exact capabilities. Preserve genuine safety/review rules. A conflict needs a bounded record and resolver, not silently weakened gates.

## Execute

Continue the existing dependency plan and one bounded observable workflow. M01's current handoff maps what was actually inspected and what remains unknown. Do not create a second onboarding, lifecycle, money or assignment engine to bypass missing local evidence. Restore access to the relevant reviewed source first; only then repair an evidenced missing link.

Before a change record requirement/section, exact source files/functions, schema/worker dependencies, acceptance and failure/recovery cases, owner and non-goals. Run the smallest relevant tests plus applicable gates. Unit mocks, skipped DB tests, seeded terminal rows, compile success and HTTP health are not end-to-end evidence. Missing DATABASE_URL means DB tests unexecuted, not passed. Preserve auth, idempotency, version, identity/access and external-effect containment.

Toolchain provenance: .github/workflows/ci.yml selects Node 22. The inspected package.json does not declare an engines or packageManager pin; do not attribute Node/npm requirements to it. The September 20 baseline CI logs record Node 22.23.2 and npm 10.9.8; the assistant environment used Node 22.16.0 and npm 10.9.2. These are observed versions, not equivalent-environment certification. Capture actual versions on every run.

For CI parity, capture each command/result separately: npm ci; npx tsc --noEmit; npx eslint backend/src/ --ext .ts --max-warnings 0; npx vitest run --reporter=verbose; npm audit --omit=dev --audit-level=high; npm run compile. The package lint script alone does not add CI's zero-warning argument. Existing CI reads TEST_* database/Redis secrets; their names do not prove disposable topology or effect containment. Local application startup and db:validate require a validated disposable environment; never run db:reset:destructive on shared/persistent targets. Inspect actual external-effect boundaries before starting API/workers; no live credentials for synthetic tests.

## Report and stop

Report VERIFIED WORKING only for the demonstrated scope. Use PARTIAL, BROKEN, PHANTOM, DEAD/REDUNDANT or UNKNOWN with evidence; production blocker is separate. Record commands, exact SHA/configuration, skips/failures, artifact access, independent review and deployment separately. Never invent paid outcomes, availability, approval or reviewer acceptance.

Stop only the action actually blocked by missing permission/input; continue independently safe authorized work. Do not spend the session rewriting all policy documents. No default-branch merge when deployment consequences are unresolved. Before handoff supply an accessible patch/PR, exact first blocker and next resolving action; LOCAL_ONLY paths do not suffice. Material updates go to the existing shared log, not a new authority surface.
