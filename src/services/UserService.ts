/**
 * UserService — stub for src/ context.
 * The full implementation lives in backend/src/services/ or backend/src/routers/.
 */

export const UserService = {
  getStripeConnectId: async (_userId: string): Promise<string | undefined> => {
    return undefined;
  },
};
