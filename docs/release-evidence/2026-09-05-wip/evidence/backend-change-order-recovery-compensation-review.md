# Worker compensation winner checkpoint

The restricted worker can now create or replay the exact ChangeOrder compensation winner after a successful admitted fake ADJUST and permanent participant/domain revocation. The sealed command binds the current database/release, live lease, immutable witness, Work Order and ADJUST event. A committed amendment or existing compensation winner takes precedence.

New claims lock the proposal, fulfillment facts and sorted current principals before classifying revocation. Financial advisory locks use try-locks to avoid waiting in reverse order. A separate append-only origin records the worker, release, target, lease, witness and ADJUST evidence atomically with the winner. The historical participant remains requested_by. Existing legacy winners keep a null worker origin; this port does not silently adopt them.

The winner carries the deterministic reversal operation and idempotency identity, N+2 slot, base scope and exact amount/currency. Final SQL lease-expiry validation rolls back both records if persistence waited past the lease. This command does not create a durable REVERSAL request, record cancellation, restore security, or authorize capture, execution or assignment.

Validation: 246 unit tests across seven files and 263 PostgreSQL/Redis tests across four complete financial/readiness cohorts pass with zero failures, skips or todos. Compilation, expanded test types and zero-warning source lint pass. Two fresh databases reproduce 153 functions and 345 explicit triggers; transactionally reconstructed predecessor catalogs match the previous verified capture. Frozen H146/v12/seal migration bytes and the prior v13 prefix are unchanged.

Nine new PostgreSQL cases cover banned-customer and public delegate-owned recovery, restored-authority replay, amendment precedence, legacy null origins, principal/membership/financial lock races, atomic rollback and natural lease expiry while provenance insertion is blocked. Three readiness cases reject a missing private origin foreign key, disabled internal FK trigger and changed timestamp default. The 36 new adapter units validate closed receipt shapes, identities, provenance, immutability and installed authority changes.

The independent read-only agent review found no further blocking SQL/security issue after corrections. It does not replace required human release approval. JSON timestamp strings retain their microsecond bytes; JavaScript comparisons retain millisecond precision. SQL enforces exact lease expiry. Runtime identity composition and historical fixtures remain synthetic; normal signed process startup and scheduled-worker recovery are not established by this checkpoint.

The 263-case result combines three unchanged passing cohorts from the initial run with a full 19-case readiness rerun. The initial run had 262 passes and one failed assertion because the new test helper collected custody diagnostics but omitted internal FK diagnostics; readiness itself correctly refused the damaged trigger. The only correction adds that diagnostic query to the test collector. An inverse-byte check proves application code and all other tests stayed unchanged. The rerun passes all 19 readiness cases, and expanded test types pass. Both original and rerun logs are retained in the [composed cohort evidence](backend-change-order-recovery-compensation-cohort-evidence.json).

The latest complete required gate still reports 27 failures and 33 skips on earlier source. This checkpoint does not replace it. Next are worker-origin-derived durable REVERSAL preparation/requests, terminal writers and races, remaining compensation observations, scheduled-worker integration, remaining callers and current-Docs product behavior. All thirteen end-to-end journeys, clean clones, signing, independent review and isolated staging remain required. Production effects NONE; customer money and hard assignment FROZEN.

Recorded 2026-09-06T02:16:15.061Z. [Exact manifest](backend-change-order-recovery-compensation-review-manifest.json). [Incremental patch](backend-change-order-recovery-compensation-review.patch).
