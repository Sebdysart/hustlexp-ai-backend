# Current runtime authority and recovery upgrade checkpoint

Recorded 2026-09-06T20:18:06.575Z. Backend incomplete; goal active. No application runtime behavior or SQL bytes changed in this checkpoint.

The old runtime suite now provisions the current eight-role authority and executes real API snapshots. It retains PostgreSQL identity checks, all role-safety checks, primitive/deferred guard custody, live identity mutation and restoration, view/rewrite drift, trigger/transitive function body drift, elevated function ownership, pgcrypto membership/safety/ownership, direct table mutation denials, forbidden relation privileges, missing owner dependencies, missing required wrapper grants, and elevated membership. Active API/worker/attester sessions are now explicitly denied retained legacy recovery wrappers and direct digest calls. The old positive API digest expectation is updated to the current explicit prohibition; no runtime grants were expanded.

The two function-body mutations are detected earlier by current command authority: trigger-catalog mismatch for the trigger body, function-catalog mismatch for the transitive helper. Both tests assert the exact catalog reason, readiness refusal and recovery after restoring the original body.

The historical compensation replay assertions were preserved in a separate upgrade test: exact preparation, dispatch-attempt and finalization replay; same operation/request identity and one immutable outcome. It builds the historical pre-v13 state from exact registered SQL, then runs the full canonical migration command with all 14 supplemental registrations: the first 13 are already applied and v13 is applied. The historical two-role installation still refuses current readiness. This is historical migration/recovery coverage, not an active runtime recovery capability. Cleanup failures now fail the test.

Validation: **18/18 current runtime-authority cases and 1/1 historical recovery-upgrade case pass**. With the unchanged earlier 25 readiness cases, this is **44 unique PostgreSQL cases across four complete files**. Expanded types, changed-file lint (zero warnings), and whitespace checks pass. The first run produced two obsolete status expectations; the intermediate run proved one exact catalog reason and exposed the distinct trigger-catalog reason; the final complete runtime file passes. Earlier failing reports are retained.

Full required validation is **RUNNING** in the isolated checkout, session **85303**, PID 30476, started 2026-09-06T20:13:09.302Z. Snapshot: **1808 source entries / 1917 total tracked-or-unignored entries**, containing six reviewed source changes from public 1da48ae. The main worktree is not frozen. Do not run overlapping PostgreSQL tests while the full required suite owns the shared local services. Poll the same handle; a timeout is not completion. The runner archives the final JSON report and checks snapshot drift on terminal exit.

The last completed full gate remains **11,012 passed, 27 failed, 33 skipped across 696 files; FAIL**. Do not subtract focused fixes from that archived result. The public branch remains **1da48ae**; the six source changes are local and have been copied exactly into the isolated validation checkout. No commit, push, deployment or production effect occurred. Customer money and hard assignment remain frozen.

Next engineering: prepared-financial-command-authority.pg.test.ts has five cases still calling prepare without actor attestation and creates unclaimed TaskDrafts with users lacking canonical Firebase subjects. Migrate it to complete isolated authority, real API/attester commands and a claimed actor-bound draft. Preserve six-way concurrency, idempotency/context/lifecycle conflicts, dispatch-before-adapter refusal, database-owned time, approved-provider denial and immutable mutation guards. Use independent API connections for concurrent transactions; a single shared pg.Client cannot prove concurrency. Existing createSyntheticActorAttestation helper supplies the real in-process attester application and sealed PostgreSQL issuance/consumption; synthetic bearer verification is explicit. Current full snapshot contains the old preparation file, so later primary changes must not be attributed to that run.

Remaining objective is unchanged: actual financial/estimate/history and provider-account/terminal callers, updated Docs behavior, thirteen journeys, process/restart/restore/clean-clone proof and independent release/staging gates.
