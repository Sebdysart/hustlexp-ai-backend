# In-app notification producer inventory

Source audit of the feature branch, 2026-09-19. Entries include canonical/native producers and wrappers; dynamic inputs are expanded by their caller rows. This is source evidence, not a claim that every optional native producer is enabled in deployment. No notification types were added.

The final web destinations, authority, stale-state behavior and limitations are in [the audit report](provider-os-production-readiness.md). Raw paths below are intentionally shown: business quote/assessment writes pass through BusinessNotificationDestination; legacy/native paths pass through WebNotificationDestination at read time. Unsupported native-only routes have no web action.

| Source | Type/category | Recipient | Title | Message | Entity context | Raw destination |
|---|---|---|---|---|---|---|
| backend/src/jobs/EscrowActionRestriction.ts:43 | delegated input | current authorized Ops/admin set | 'Escrow Locked: Stripe Account Restricted' | `Escrow ${input.escrowId} could not be released — worker Stripe account is restricted (code: ${input.stripeCode}). Manual admin review required.` | metadata: { escrow_id: input.escrowId, worker_id: input.workerId, stripe_code: input.stripeCode, } | `/admin/escrows/${input.escrowId}` |
| backend/src/jobs/biometric-analyzer-worker.ts:179 | delegated input | current authorized Ops/admin set | `🔬 Biometric Review: ${analysis.recommendation === 'reject' ? 'REJECT' : 'Manual Review'}` | `Proof ${proof_id} flagged for ${analysis.recommendation}. Risk: ${analysis.scores.risk_level}. Flags: ${analysis.flags.join(', ')}. ${analysis.reasoning}` | metadata: { proofId: proof_id, recommendation: analysis.recommendation, riskLevel: analysis.scores.risk_level, flags: analysis.flags, reasoning: analysis.reasoning, } | `app://admin/proof-review/${proof_id}` |
| backend/src/jobs/completion-release-orchestrator.ts:61 | delegated input | current authorized Ops/admin set | 'Completion release blocked: escrow not FUNDED' | `Task ${taskId} is COMPLETED but escrow ${escrow.id} is ${escrow.state}. Manual review required.` | metadata: { escrow_id:escrow.id,task_id:taskId,escrow_state:escrow.state } | `/admin/escrows/${escrow.id}` |
| backend/src/jobs/completion-release-orchestrator.ts:278 | delegated input | current authorized Ops/admin set | 'Payout blocked: destination is not current' | `Task ${taskId} completed; escrow ${escrow.id} remains FUNDED because its payout evidence is not current. ` + `Reconcile the provider destination before release.` | metadata: { escrow_id: escrow.id, task_id: taskId, worker_id: task.worker_id, business_fulfiller_organization_id: task.business_fulfiller_organization_id, payout_recipient_user_id: payoutRecipientUserId, payout_block_reason: destination.reason, } | `/admin/escrows/${escrow.id}` |
| backend/src/jobs/completion-release-orchestrator.ts:319 | delegated input | current authorized Ops/admin set | 'Payout blocked: Stripe account restriction' | `Completion release for escrow ${escrow.id} hit restriction '${code}'. Manual review required.` | metadata: { escrow_id:escrow.id,task_id:taskId,worker_id:task.worker_id,payout_recipient_user_id:payoutRecipientUserId,stripe_code:code } | `/admin/escrows/${escrow.id}` |
| backend/src/jobs/email-worker.ts:645 | delegated input | current authorized Ops/admin set | 'Email delivery permanently failed' | `emailId ${emailId} (type: ${emailType}) exhausted all retries. Manual review required.` | metadata: { emailId, emailType, attempts: currentAttempts, maxAttempts, lastError: errorMessage } | 'app://admin/email-outbox' |
| backend/src/jobs/instant-notification-worker.ts:154 | 'instant_task_available' | hustlerId | title | body | taskId: taskId; metadata: { instantMode: true, riskLevel, sensitive: sensitive \|\| false, surgeLevel: surgeLevel \|\| 0, // Track when notification was created for latency metrics notifiedAt: new Date().toISOString(), } | `/tasks/${taskId}/accept` |
| backend/src/jobs/payment-worker.ts:641 | delegated input | current authorized Ops/admin set | 'charge.refunded: unroutable refund event' | criticalMsg | metadata: { charge_id: charge.id, stripe_event_id: stripeEventId } | `/admin/stripe-events/${stripeEventId}` |
| backend/src/jobs/payment-worker.ts:796 | delegated input | current authorized Ops/admin set | 'charge.refunded: transfer reversal failed' | `Escrow ${phase1Escrow.id} — reversal of transfer ${phase1Escrow.stripe_transfer_id} failed: ${reversalResult.error.message}. Worker may retain funds. Manual reconciliation required.` | metadata: { escrow_id: phase1Escrow.id, stripe_transfer_id: phase1Escrow.stripe_transfer_id, stripe_event_id: stripeEventId } | `/admin/escrows/${phase1Escrow.id}` |
| backend/src/jobs/payment-worker.ts:811 | delegated input | current authorized Ops/admin set | 'charge.refunded: transfer reversal threw' | `Escrow ${phase1Escrow.id} — reversal of transfer ${phase1Escrow.stripe_transfer_id} threw an error. Worker may retain funds. Manual reconciliation required.` | metadata: { escrow_id: phase1Escrow.id, stripe_transfer_id: phase1Escrow.stripe_transfer_id, stripe_event_id: stripeEventId } | `/admin/escrows/${phase1Escrow.id}` |
| backend/src/jobs/payment-worker.ts:1354 | 'payout_failed' | userId | 'Bank Transfer Failed' | 'Your bank transfer failed. Please update your bank details in the app to receive your earnings.' | objectRef: { type: 'payout', id: payoutId }; metadata: { payoutId, stripeEventId, eventVersion: 1 } | 'app://settings/payouts' |
| backend/src/jobs/payout-event-worker.ts:130 | state === 'paid' ? 'payment_released' : 'payout_failed' | workerId | state === 'paid' ? 'Bank Payout Paid' : 'Bank Payout Failed' | state === 'paid' ? 'Stripe marked your bank payout paid. Open your wallet for the provider-backed receipt.' : 'Your bank payout failed. Stripe returns failed payout funds to the connected balance; update the destination before trying again.' | objectRef: { type: 'payout', id: payoutId }; metadata: { payoutId, stripeEventId, eventVersion: 1 } | `app://wallet/${payoutId}` |
| backend/src/jobs/stripe-event-worker.ts:427 | 'security_alert' | userId | 'Action Required: Stripe Connect' | 'Your Stripe Connect account requires additional information. Please update your details to continue receiving payments.' | — | 'app://settings/payments' |
| backend/src/jobs/worker-registration.ts:25 | funded ? 'escrow_funded' : 'refund_issued' | posterId | funded ? 'Payment Captured' : 'Refund Processed' | funded ? 'Your payment was captured. The task is now funded and ready to be accepted.' : 'Your payment has been refunded. Funds will appear in your account within a few business days.' | taskId: taskId; objectRef: { type: 'escrow', id: escrowId }; metadata: { escrowId, eventVersion: job.data.event_version } | `/tasks/${taskId}` |
| backend/src/jobs/worker-registration.ts:50 | 'payment_failed' | posterId | 'Payment Failed' | 'Your payment could not be processed. Please update your payment method and try again.' | taskId: taskId; objectRef: { type: 'escrow', id: escrowId }; metadata: { escrowId, eventVersion: job.data.event_version } | `/tasks/${taskId}` |
| backend/src/jobs/worker-registration.ts:69 | 'payout_failed' | workerId | 'Payment Transfer Failed' | 'Your payment transfer failed. Our team has been alerted and will resolve this urgently. No action required from you.' | objectRef: { type: 'escrow', id: escrowId }; metadata: { escrowId, eventVersion: job.data.event_version } | '/earnings' |
| backend/src/jobs/xp-tax-reminder-worker.ts:92 | 'payment_due' | user.user_id | 'XP Tax Payment Due' | `You have $${taxAmount} in unpaid XP taxes. ${user.total_xp_held_back} XP is being held back until payment. Pay now to unlock your XP!` | metadata: { unpaidTaxCents: user.total_unpaid_tax_cents, xpHeldBack: user.total_xp_held_back, } | 'app://settings/xp-tax' |
| backend/src/lib/task-lifecycle-notifications.ts:21 | delegated input | caller / delegated recipients | — | — | — | — |
| backend/src/lib/task-lifecycle-notifications.ts:32 | 'new_matching_task' | posterId | 'New applicant' | `Someone applied to "${taskTitle}". Review applicants and assign.` | taskId: taskId | `/tasks/${taskId}/applicants` |
| backend/src/lib/task-lifecycle-notifications.ts:45 | 'task_accepted' | workerId | "You're assigned!" | `You got "${taskTitle}". Head over and start when ready.` | taskId: taskId | `/tasks/${taskId}` |
| backend/src/lib/task-lifecycle-notifications.ts:58 | 'task_accepted' | posterId | 'Your task was accepted' | `A hustler accepted "${taskTitle}" and is on it.` | taskId: taskId | `/tasks/${taskId}` |
| backend/src/lib/task-lifecycle-notifications.ts:71 | 'proof_submitted' | posterId | 'Proof submitted — review needed' | `Work on "${taskTitle}" is done. Review the proof to release payment.` | taskId: taskId | `/tasks/${taskId}/proof` |
| backend/src/lib/task-lifecycle-notifications.ts:84 | 'proof_rejected' | workerId | 'Proof needs another pass' | reason ? `"${taskTitle}": ${reason}` : `Your proof for "${taskTitle}" was not approved. Check feedback and resubmit.` | taskId: taskId | `/tasks/${taskId}/proof` |
| backend/src/lib/task-lifecycle-notifications.ts:97 | 'task_completed' | workerId | 'Task approved 🎉' | `"${taskTitle}" is complete. Your payout is ready for processing.` | taskId: taskId | `/tasks/${taskId}` |
| backend/src/lib/task-lifecycle-notifications.ts:111 | 'payment_released' | workerId | 'Payout released' | `$${dollars} was released to your payout account. Check earnings for settlement status.` | taskId: taskId | `/earnings` |
| backend/src/routers/admin.ts:285 | delegated input | current authorized Ops/admin set | 'Escrow Refund Failed on User Ban' | `Escrow ${escrow.id} failed to refund when user ${input.userId} was banned — manual intervention required` | metadata: { escrowId: escrow.id, userId: input.userId, error: refundResult.error?.message } | `hustlexp://admin/escrows/${escrow.id}` |
| backend/src/routers/admin.ts:406 | delegated input | current authorized Ops/admin set | 'Escrow Refund Failed on User Suspension' | `Escrow ${escrow.id} failed to refund when user ${input.userId} was suspended — manual intervention required` | metadata: { escrowId: escrow.id, userId: input.userId, error: refundResult.error?.message } | `hustlexp://admin/escrows/${escrow.id}` |
| backend/src/routers/businessAssessment.ts:29 | 'ASSESSMENT_PAID' | organization a.business_organization_id | 'Assessment fee paid' | 'The customer paid the onsite assessment fee.' | entityType: 'assessment'; entityId: a.id | `/business/claims/${a.task_draft_id}` |
| backend/src/routers/businessAssessment.ts:87 | 'ASSESSMENT_SCHEDULED' | organization assessment.business_organization_id | 'Assessment scheduled' | 'The customer selected a date for the onsite assessment.' | entityType: 'assessment'; entityId: assessment.id | `/business/claims/${assessment.task_draft_id}` |
| backend/src/routers/businessAssessment.ts:132 | 'ASSESSMENT_COMPLETED' | result.rows[0].poster_user_id | 'Assessment completed' | 'The onsite assessment is complete. The business can now prepare your quote.' | entityType: 'assessment'; entityId: result.rows[0].id | `/dashboard/drafts/${result.rows[0].task_draft_id}` |
| backend/src/routers/quoteDecision.ts:320 | 'QUOTE_ACCEPTED' | organization quote.business_organization_id | 'Quote accepted' | 'The customer accepted your quote.' | entityType: 'quote'; entityId: input.quoteId | quote.acquisition_origin === 'provider_os' ? `/provider-os/quotes/${input.quoteId}?organizationId=${quote.business_organization_id}` : `/business/claims/${input.taskDraftId}` |
| backend/src/routers/quoteDecision.ts:422 | 'QUOTE_REJECTED' | organization organizationId | 'Quote declined' | 'The customer declined your quote.' | entityType: 'quote'; entityId: input.quoteId | quoteOwner.rows[0]?.acquisition_origin === 'provider_os' ? `/provider-os/quotes/${input.quoteId}?organizationId=${organizationId}` : `/business/claims/${input.taskDraftId}` |
| backend/src/routers/support.ts:32 | 'SUPPORT_REQUEST_CREATED' | current authorized Ops/admin set | 'New support request' | input.subject | entityType: 'support_thread'; entityId: threadId | `/ops/support/${threadId}` |
| backend/src/routers/support.ts:125 | 'SUPPORT_USER_REPLY' | current authorized Ops/admin set | 'New support reply' | 'A user replied to a support conversation.' | entityType: 'support_thread'; entityId: input.threadId | `/ops/support/${input.threadId}` |
| backend/src/routers/web/ops.ts:412 | 'ASSESSMENT_APPROVED' | draftContext.poster_user_id | 'Assessment ready' | assessmentFeeCents && assessmentFeeCents > 0 ? 'The onsite assessment was approved. Pay the assessment fee to choose a date.' : 'The onsite assessment was approved. Choose a date for the visit.' | entityType: 'assessment'; entityId: result.rows[0].id | `/dashboard/drafts/${draftContext.task_draft_id}` |
| backend/src/routers/web/ops.ts:452 | 'ASSESSMENT_REJECTED' | organization assessmentContext.business_organization_id | 'Assessment request declined' | 'HustleXP did not approve the onsite assessment request.' | entityType: 'assessment'; entityId: result.rows[0].id | `/business/claims/${assessmentContext.task_draft_id}` |
| backend/src/routers/web/ops.ts:2024 | 'TASK_PROPOSAL_RECEIVED' | organization input.business_organization_id | 'New task proposal' | draft.title ? `HustleXP invited your business to review and quote "${draft.title}".` : 'HustleXP invited your business to review and quote a new task.' | entityType: 'business_task_proposal'; entityId: proposal.id | `/business/proposals/${proposal.id}` |
| backend/src/routers/web/ops.ts:2105 | 'SUPPORT_OPS_REPLY' | thread.opened_by_user_id as string | 'HustleXP support replied' | 'HustleXP replied to your support request.' | entityType: 'support_thread'; entityId: input.id as string | actionUrl |
| backend/src/services/AdminNotificationHelper.ts:86 | 'security_alert' | adminId | params.title | params.body | metadata: params.metadata | params.deepLink |
| backend/src/services/BusinessAssessmentService.ts:109 | 'ASSESSMENT_REQUESTED' | current authorized Ops/admin set | 'New assessment request' | `${org.display_name \|\| 'A business'} requested an onsite assessment.` | entityType: 'assessment'; entityId: assessmentRequestId | `/ops/drafts/${draft.id}` |
| backend/src/services/BusinessAssessmentService.ts:365 | 'ASSESSMENT_REQUESTED' | current authorized Ops/admin set | 'New assessment request' | `${org.display_name \|\| 'A business'} requested an onsite assessment.` | entityType: 'assessment'; entityId: assessmentRequestId | `/ops/drafts/${draft.id}` |
| backend/src/services/BusinessNotificationDigestService.ts:108 | 'business_operational_digest' | row.user_id | `Weekly operations — ${row.display_name}` | `Last week: ${completedCount} completed, ${cancelledCount} cancelled, ${disputedCount} disputed. Now: ${activeCount} active, ${upcomingCount} upcoming.` | objectRef: { type: 'business_week', id: `${row.organization_id}:${week}` }; metadata: { organizationId: row.organization_id, periodStart: window.start.toISOString(), periodEnd: window.end.toISOString(), completedCount, cancelledCount, disputedCount, activeCount, upcomingCount, } | `/business/${row.organization_id}/operations?week=${week}` |
| backend/src/services/BusinessQuoteActivationService.ts:35 | 'QUOTE_RECEIVED' | input.posterUserId | 'New quote received' | 'A business sent you a quote.' | entityType: 'quote'; entityId: input.quoteId | `/dashboard/drafts/${input.taskDraftId}` |
| backend/src/services/ContentModerationService.ts:457 | 'security_alert' | queueItem.user_id | 'Content Removed' | `Your ${queueItem.content_type} has been removed after review. ${reviewNotes ? `Reason: ${reviewNotes}` : ''}` | taskId: queueItem.content_type === 'task' ? queueItem.content_id : undefined; metadata: { queueItemId: queueItem.id, decision, reviewNotes } | `app://content/${queueItem.content_id}` |
| backend/src/services/ContentModerationService.ts:476 | delegated input | current authorized Ops/admin set | '📋 Content Escalated for Admin Review' | `${queueItem.content_type} (ID: ${queueItem.content_id}) by user ${queueItem.user_id} has been escalated for admin review.${reviewNotes ? ` Notes: ${reviewNotes}` : ''}` | metadata: { queueItemId: queueItem.id, contentType: queueItem.content_type, contentId: queueItem.content_id, reviewNotes } | `app://admin/moderation/${queueItem.id}` |
| backend/src/services/ContentModerationService.ts:818 | 'security_alert' | appeal.user_id | 'Appeal Successful' | 'Your appeal has been reviewed and your content has been restored.' | metadata: { appealId: appeal.id, originalDecision: appeal.original_decision } | appeal.moderation_queue_id ? `app://moderation/${appeal.moderation_queue_id}` : 'app://profile' |
| backend/src/services/EarnedVerificationUnlockService.ts:128 | 'EARNED_VERIFICATION_UNLOCKED' | userId | 'Verification Unlocked' | 'You have earned enough to unlock identity verification for $1. Contact support to upgrade.' | entityType: 'user'; entityId: userId; metadata: { action: 'OPEN_VERIFICATION', fee_cents: 100 } | '/support' |
| backend/src/services/EscrowRefundTransaction.ts:123 | delegated input | current authorized Ops/admin set | 'REFUND RACE CONDITION — Manual Reconciliation Required' | `Escrow ${context.escrowId}: release() raced with refund(). Stripe refund ${stripeRefundId} already issued (charge: ${stripeChargeId}). Worker received transfer AND poster received refund. Amount: ${context.amount} cents. Investigate immediately.` | metadata: { escrow_id: context.escrowId, stripe_refund_id: stripeRefundId, stripe_charge_id: stripeChargeId, amount_cents: context.amount, } | `/admin/escrows/${context.escrowId}` |
| backend/src/services/ExpertiseSupplyService.ts:1379 | 'new_matching_task' | waiter.user_id | '🎯 Expertise Slot Available!' | `A slot has opened up for "${expertiseId}" in your area. Accept within ${WAITLIST_INVITE_EXPIRY_HOURS} hours before it expires.` | metadata: { waitlistId: waiter.id, expertiseId, geoZone, expiresAt: expiresAt.toISOString(), } | `app://expertise/invite/${waiter.id}` |
| backend/src/services/FraudDetectionService.ts:617 | delegated input | current authorized Ops/admin set | '🚨 CRITICAL Fraud Pattern Detected' | `Pattern "${patternType}" detected for ${userIds.length} user(s): ${userIds.join(', ')}. Accounts have been auto-suspended. Review in admin dashboard.` | metadata: { patternType, userIds, riskLevel, patternId: result.rows[0]?.id } | 'app://admin/fraud' |
| backend/src/services/FraudDetectionService.ts:627 | 'account_suspended' | userId | 'Account Suspended' | 'Your account has been temporarily suspended due to detected fraudulent activity. Please contact support to appeal.' | metadata: { patternType, patternId: result.rows[0].id } | 'app://support' |
| backend/src/services/FraudDetectionService.ts:687 | delegated input | current authorized Ops/admin set | '⚠️ HIGH Risk Fraud Pattern' | `Pattern "${patternType}" detected for ${userIds.length} user(s): ${userIds.join(', ')}. Accounts flagged — manual review required.` | metadata: { patternType, userIds, riskLevel, patternId: result.rows[0]?.id } | 'app://admin/fraud' |
| backend/src/services/GDPRService.ts:801 | 'security_alert' | request.user_id | 'Account Deletion Completed' | 'Your account and personal data have been permanently deleted per your GDPR request. This action cannot be undone.' | metadata: { requestId, deletedAt: deletedAt.toISOString() } | 'app://support' |
| backend/src/services/MessagingDeliveryService.ts:140 | 'message_received' | recipientId | 'New Message' | body | taskId: taskId; metadata: { messageId: message.id, messageType, senderId } | `app://task/${taskId}/messages` |
| backend/src/services/NotificationService.ts:472 | delegated input | caller / delegated recipients | — | — | — | — |
| backend/src/services/NotificationService.ts:487 | delegated input | userId | — | — | — | — |
| backend/src/services/NotificationService.ts:500 | delegated input | userId | — | — | — | — |
| backend/src/services/ProviderOsPremiumEvents.ts:145 | `PROVIDER_OS_${event.event_type}` | recipient.id | 'Provider OS update' | premiumMessage(event.event_type) | entityType: 'provider_os_event'; entityId: event.id | premiumDestination(event) |
| backend/src/services/QuotePaymentFinalizationService.ts:806 | 'PAYMENT_CONFIRMED' | context.poster_user_id | 'Payment confirmed' | 'Your payment was confirmed and the task is ready.' | entityType: 'task'; entityId: materialized.taskId | `/dashboard/tasks/${materialized.taskId}` |
| backend/src/services/QuotePaymentFinalizationService.ts:818 | 'CUSTOMER_PAYMENT_RECEIVED' | organization context.business_organization_id | 'Customer payment confirmed' | 'The customer completed payment. The task is ready to begin.' | entityType: 'task'; entityId: materialized.taskId | `/business/tasks/${materialized.taskId}` |
| backend/src/services/StripeService.ts:713 | delegated input | current authorized Ops/admin set | 'Stripe Event Permanently Stuck' | `Stripe event ${eventId} is permanently stuck — idempotency row deletion failed. Manual DB intervention required.` | metadata: { event_id: eventId, delete_error: deleteError instanceof Error ? deleteError.message : String(deleteError) } | `/admin/stripe-events/${eventId}` |
| backend/src/services/TippingService.ts:371 | 'tip_received' | tip.worker_id | '💰 You received a tip!' | `You received a $${(tip.amount_cents / 100).toFixed(2)} tip! Great job!` | entityType: 'tip'; entityId: tip.id; metadata: { task_id: tip.task_id, amount_cents: tip.amount_cents } | `/tasks/${tip.task_id}` |

62 concrete call sites, including shared wrappers. Account-bound/dynamic metadata is shown as source expressions, never live customer data. Former raw SQL producers (tip and earned-verification unlock) now appear as shared-writer calls.
