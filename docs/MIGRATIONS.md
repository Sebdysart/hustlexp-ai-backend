# PostgreSQL schema and migrations

Status: `CURRENT_IMPLEMENTATION_REFERENCE / MIGRATION_SELECTION_HOLD`

Production effects authorized: `NONE`

Read [the Team Goal and Execution Contract](HUSTLEXP_TEAM_ALIGNMENT.md) and the source-dated [Current Backend Checkpoint](HUSTLEXP_CURRENT_BACKEND_CHECKPOINT.md). The last canonical diagnostic reports node `TASK_FIRST_FAKE_FSE_POSTGRES_AUTHORITY_REBUILD`, but the current Governor preflight fails and grants no execution admission. Both exact-name conditional paths are `LOCKED`. `20260824_task_first_fake_fse_vertical` has a prospective clean-baseline append ordinal of 112 that is explicitly not authorized. `20260825_task_first_fake_fse_postgres_authority_repair` has no assigned source ordinal pending legacy-chain reconstruction. The persisted ledger identity is the migration name; no numeric alias identifies a persisted migration. Do not create, rename, register, apply, or choose either path until every relevant persistent PostgreSQL target is closed with admissible evidence and a fresh canonical revision lock selects one exact name and assigns its source ordinal.

That lock is specific to the two named task-first money paths and to all persistent-target execution. The Universal V1 working mission permits unrelated append-only contracts to be authored and exercised against explicitly disposable local databases as review candidates. This source-dated dirty local review snapshot has 140 ordered registry entries. Its Universal V1 tail is `20260903_universal_v1_task_draft_account_claim` at source ordinal 112, `20260904_canonical_user_email_identity` at 113, `20260905_universal_v1_task_draft_legacy_claim_import_repair` at 114, `20260906_universal_v1_estimate_acceptance_materialization` at 115, `20260907_universal_v1_provider_estimate_invitation` at 116, `20260908_universal_v1_provider_work_order_authority` at 117, `20260909_universal_v1_reconciliation_alias_repair` at 118, `20260911_universal_v1_change_order_application` at 119, `20260912_universal_v1_work_order_execution_facts` at 120, `20260913_universal_v1_completion_delivery_receipt` at 121, `20260914_notification_provider_in_flight` at 122, `20260915_ai_spend_attempt_ledger` at 123, `20260916_provider_event_inbox_v1` at 124, `20260917_financial_provider_command_journal_v1` at 125, `20260918_universal_v1_prepared_financial_command_v1` at 126, `20260919_provider_event_processing_v1` at 127, `20260920_financial_provider_command_recovery_v1` at 128, `20260923_legacy_escrow_insert_containment_v1` at 129, `20260924_universal_v1_task_draft_route_context_v1` at 130, `20260925_universal_v1_work_order_compensation_v1` at 131, `20260928_provider_observation_normalization_v1` at 132, `20260929_universal_v1_double_entry_ledger_v1` at 133, `20260930_universal_v1_ops_cases_v1` at 134, `20261001_universal_v1_relationship_origin_v1` at 135, `20261002_universal_v1_dispute_recovery_v1` at 136, `20261003_universal_v1_task_opportunities_v1` at 137, `20261004_universal_v1_completion_notice_dispatch_v1` at 138, `20261005_universal_v1_occurrence_access_audit_v1` at 139, and `20261006_stage1_legacy_authority_containment_v1` at 140. The exact SHA-256 artifact digest for the current 140-entry engine registry and complete eight-fixture migration directory is `59e5eae7fa8d7c0327b0e2f803d3f040223a03025e16de159b89090d62a8062a`; migration-140 SQL SHA-256 is `5cc6f5710696048b98c97ea06993a56aefaf711bac7a04dd973059f957d4eb63`. Migration-138 SQL SHA-256 `f5723941769d027e496d0a3a7fed32b29b1a02ff76db97ead815acd46a7fdee8` and migration-139 SQL SHA-256 `d805ce946b7aba804be55aea9f131cff99047adaa7d46cd3d21242ac492ec364` remain frozen. The earlier `6d1222a3eab5f1022f97ea942827a8869d63c829dd2638c0c94d52fc8c66d0be` digest remains historical evidence for the 139-entry/eight-fixture directory snapshot, `b84faf2ea97375c6fb55e40a58660ab6401be871edc5625c93609e85456246d3` remains historical evidence for the frozen 138-entry/eight-fixture snapshot, `25d65e5bd661cce785211afb9e89ac92f4b2135f07be0b9bff43884b33b2064e` remains historical evidence for the prior 137-entry/eight-fixture snapshot, and `81fa4e0afabde598bf91dc3e0ce604a3c88c550a1138d69ae8b3efa8e10cf068` remains historical evidence for the prior 130-entry/v5 snapshot; none is a digest of these current bytes.

These entries are dirty local review-branch working bytes on `codex/universal-v1a-canonical-reconstruction-20260830` and are explicitly nonpromotable. The configured GitHub remote is used only for an `EVIDENCE_ONLY` developer-review checkpoint; this lineage is not a merge source and has no immutable signed recovery candidate, hosted checks, independent approval, persistent-database, merge, deployment, or production authority. The registered append-only contracts implement payment-free estimate invitation and acceptance, provider interest and eligibility, an unassigned fake-secured Work Order, change orders, execution, evidence and completion, durable provider-event and command evidence, a DB-validated fake-only `PREPARED` lifecycle snapshot, append-only provider-event processing, fenced reconciliation-only command-recovery evidence, a synthetic completion-notice dispatch request, and purpose-bound immutable audit evidence for every Operations occurrence projection. PREPARED and REQUESTED grant neither provider I/O nor lifecycle DML.

The post-engine, nonproduction-only fake-finance chain contains eight ordered fixtures and remains outside the engine registry. `20260921_universal_v1_fake_financial_lifecycle_bridge_v1` is fixture four, records `hxos_fake_financial_schema_evidence_v4`, and binds an immutable command chain to its fake-provider event, terminal outcome, and controlled-test lifecycle event. `20260922_universal_v1_fake_terminal_lifecycle_intent_v1` is fixture five, records `hxos_fake_financial_schema_evidence_v5`, and preserves an immutable terminal plan before the first fake financial effect, provider-authored current provider-account facts, and a persisted outcome-to-reconciliation snapshot-digest bridge. `20260926_universal_v1_change_order_three_phase_v1` is fixture six and records `hxos_fake_financial_schema_evidence_v6`; `20260927_universal_v1_change_order_recovery_v1` is fixture seven and records `hxos_fake_financial_schema_evidence_v7`; `20261002_universal_v1_dispute_fake_release_gate_v8` is fixture eight and records `hxos_fake_financial_schema_evidence_v8`. Fixture v8 attaches the material-dispute release gate only after the registered dispute engine and complete v7 fake chain exist; it creates no intent, provider call, money effect, refund, reversal, release, or payout. Engine migrations 129 through 140 are, in order, legacy escrow containment, TaskDraft route context, Work Order compensation, provider-observation normalization, the provider-neutral double-entry ledger, Ops Cases, RelationshipOrigin, dispute/recovery, Task Opportunities, completion-notice dispatch, purpose-bound Operations occurrence-access audit, and forward-only Stage-1 legacy-authority containment. None of `20260827_fake_financial_provider_v1`, `20260903_fake_financial_provider_account_refresh_v2`, `20260910_fake_financial_settlement_completion_v3`, `20260921_universal_v1_fake_financial_lifecycle_bridge_v1`, `20260922_universal_v1_fake_terminal_lifecycle_intent_v1`, `20260926_universal_v1_change_order_three_phase_v1`, `20260927_universal_v1_change_order_recovery_v1`, or `20261002_universal_v1_dispute_fake_release_gate_v8` is a production engine-registry migration. These facts grant no provider I/O, real-money effect, hard assignment, customer-money effect, migration authority, deployment capability, production authority, or external authority. Earlier verifier receipts remain immutable source-dated historical evidence; they are not statements about this dirty 140-entry/eight-fixture working registry.

The clean-install baselines intentionally stop before the dependency-bound registered `20260918`-`20261006` tail. `20260918` references TaskDraft, eligibility, scope, Work Order, completion, and financial-event objects established by earlier registered migrations; migrations 129–140 then add containment, route context, compensation, causal observation evidence, balanced ledger authority, Operations cases, relationship origin, dispute/recovery, privacy-safe Task Opportunities, completion-notice dispatch, purpose-bound Operations read audit, and forward-only containment of retired Stage-1 writers and bypasses. The ordered 140-entry migration runner applies that exact fifteen-migration tail after its engine prerequisites. Separately, `20260921_universal_v1_fake_financial_lifecycle_bridge_v1`, `20260922_universal_v1_fake_terminal_lifecycle_intent_v1`, `20260926_universal_v1_change_order_three_phase_v1`, `20260927_universal_v1_change_order_recovery_v1`, and `20261002_universal_v1_dispute_fake_release_gate_v8` remain outside both foundational baselines and `REQUIRED_MIGRATION_FILES`. The nonproduction bootstrap order remains fake-provider v1, account-refresh v2, settlement-completion v3, lifecycle-bridge v4, terminal-intent v5, change-order-three-phase v6, change-order-recovery v7, and dispute-release-gate v8. Applying any fake fixture as an engine migration is forbidden. Those eight SQL files remain packaged directory fixtures; the exact migration-140 verification run did not reapply them. The focused migration-140 unit cohort passed **5/5**. The focused live-PostgreSQL cohort passed **3/3**: its contaminated upgrade applied 103 common migrations, preserved twelve Stage-1 receipts and representative populated rows, and applied the 36-migration candidate suffix through ordinal 140; separate quote-partial and task-partial upgrade cases proved fail-closed rollback and preserved their ledgers. The contaminated case also proved exact raw replay, replica refusal, and partial-NULL checksum refusal. The complete engine PostgreSQL verifier proved fresh install, supported upgrade, registered replay and recovery through all 140 engine entries and emitted `HXOS_ENGINE_MIGRATIONS_POSTGRES_OK 140`. The current full-directory artifact SHA-256 is `59e5eae7fa8d7c0327b0e2f803d3f040223a03025e16de159b89090d62a8062a`; migration-140 SQL SHA-256 is `5cc6f5710696048b98c97ea06993a56aefaf711bac7a04dd973059f957d4eb63`. The frozen migration-138 and migration-139 SQL hashes and their `b84faf2ea97375c6fb55e40a58660ab6401be871edc5625c93609e85456246d3` and `6d1222a3eab5f1022f97ea942827a8869d63c829dd2638c0c94d52fc8c66d0be` verifier receipts remain historical evidence only. This local evidence grants no CI, signed-candidate, merge, staging, deployment, or production authority. It grants no payment, payout, or provider-call authority; production money remains `FROZEN`.

Migration 138 atomically creates one immutable completion-notice request, one pending EMAIL row, and one matching pending outbox event for the exact submitted completion. The nonproduction worker may cross the provider boundary only through the configured `smtp_sink`; final DATA `250` is the sink acceptance point, QUIT is best-effort, and the persisted receipt materializes exactly one canonical `SYNTHETIC_SINK` delivery fact. A pre-marker authority revocation or post-enqueue opt-out terminally suppresses the notice, holds approval, and leaves only rejection or separately authorized manual recovery—no false EMAIL receipt is fabricated. A crash after DATA acceptance but before receipt persistence is conservatively `provider_outcome_unknown` and is never blindly resent without a separately specified deterministic sink lookup. Pre-existing submitted completions are not backfilled, and a migration-121 partial-NULL audit tuple causes named upgrade refusal rather than invented repair. This seam grants no live provider, assignment, private-address, payment, capture, settlement, payout, or production capability.

Migration 139 adds a single purpose-bound Operations read-audit table and a sealed insert function. The database rechecks the named operator's current role, derives database observation time, records the exact canonical projection SHA-256, and rejects update, delete, and truncate. Ambient table/function access is revoked. This is read accountability only: it creates no lifecycle, assignment, provider, address, financial, payment, settlement, payout, or production capability.

Migration 140 is the forward-only repair for twelve retired Stage-1 migrations whose receipts and populated objects may already exist on contaminated databases. It preserves those rows, objects, and receipts as incident evidence; disables every retired relation, feature-flag compatibility writer, quote/business-claim writer, business/manual task path, and payout bypass; installs always-enabled row and truncate guards, including `BEFORE TRUNCATE` containment on `public.quotes` and `public.tasks`; restores the pre-Stage-1 worker-only acceptance and payout gates; and refuses a partial legacy task or quote shape. It does not delete or rewrite history and grants no assignment, provider, payment, payout, deployment, production, or other positive capability. Production money remains frozen.

Disposable PostgreSQL can demonstrate that lifecycle and schema behavior, but it cannot seal the callable Work Order command boundary. The machine-readable `backend/database/work-order-command-authority.HOLD.json` and [Work Order database authority boundary](architecture/HUSTLEXP_WORK_ORDER_DATABASE_AUTHORITY_BOUNDARY.md) remain `EXTERNAL_DECISION_REQUIRED / RELEASE_BLOCKING`: authenticated actor binding, role provisioning, sealed functions, direct-DML revocation, and exact live privilege readback are still required before any release claim.

Observed on 2026-08-25: the canonical Railway ledger contained 103 distinct applied migration names and did not contain the exact proposed task-first migration name. Homebrew clone-only review root `313fd8537feb1f503b11ff1459c4f63809961ce027ffdaa116c5671cf4fd25c0` (`SHA-256(SHA256SUMS)`) excludes only PostgreSQL system identifiers `7590278899115318006` and `7665324946412008239`; it does not close Colima or any other persistent target. Seven Neon project/branch/database tuples and other historical or opaque persistent targets remained unresolved.

The non-authorizing C0 and provider-unbound query packages identify no persistent Neon ledger and close no migration target. Provider identity remains `UNKNOWN`, and the entire A0 successor line remains rejected; its single exact candidate/review lineage is maintained in the [source-dated checkpoint](HUSTLEXP_CURRENT_BACKEND_CHECKPOINT.md) rather than duplicated here. No predecessor or successor package selects a path, assigns an ordinal, or authorizes provider interaction. These are source-dated observations, not permission to assume absence elsewhere.

There are two intentionally different schema artifacts:

- `backend/database/constitutional-schema.sql` is the fresh-database baseline and core invariant definition.
- `backend/database/migrations/` contains incremental production changes. The authoritative ordered subset is `backend/src/jobs/engine-automation-migration-files.ts`.

### Fake-financial foreground boundary

`20260918_universal_v1_prepared_financial_command_v1` is a registered fake-only, post-baseline review authority. `20260921_universal_v1_fake_financial_lifecycle_bridge_v1`, `20260922_universal_v1_fake_terminal_lifecycle_intent_v1`, `20260926_universal_v1_change_order_three_phase_v1`, `20260927_universal_v1_change_order_recovery_v1`, and `20261002_universal_v1_dispute_fake_release_gate_v8` are separate post-engine, nonproduction-only fixtures and are deliberately absent from the production migration registry. None makes the caller-owned transaction factory safe or callable. `authorizeUniversalV1FakeFinancialTransaction()` remains fail-closed with `UNIVERSAL_FINANCE_CALLER_OWNED_TRANSACTION_PREPARED_AUTHORITY_REFUSED`; no production-money capability is enabled.

The review-only Work Order path now implements the required three committed phases:

1. `UniversalV1WorkOrderApplication` commits one single-winner command witness after locking and validating the current TaskDraft, task, eligibility, scope, provider, estimate, and conditional hold.
2. The independent fake coordinator records exact PREPARED, REQUESTED, DISPATCH lease, DISPATCH_ATTEMPTED, raw fake-provider event, terminal outcome, lifecycle event, and `20260921` bridge evidence for `PREPARE_PAYMENT_METHOD`, `AUTHORIZE`, and `SECURE`. Crash replay reads the exact durable event and outcome; it does not blindly redispatch.
3. `PostgresUniversalV1WorkOrderRepository.finalizeMaterialization()` opens a new serializable transaction, re-locks and revalidates current authority, proves the exact secured-event bridge, and only then materializes the unassigned Work Order.

After completion, `UniversalV1FulfillmentApplication` commits and re-reads the exact business-authority snapshot, then atomically records one immutable fixed terminal plan before invoking the independent fake-only coordinator. Each crash retry resumes from that exact intent and predecessor chain rather than inferring a new plan. The coordinator proves exact Work Order-bound `CAPTURE` through bank-settlement observation, full `REFUND`, and `RECONCILE` command/evidence chains; payout requires the exact latest enabled provider-authored provider-account fact, and reconciliation binds the terminal event to a canonical snapshot digest. The final dispatch-attempt boundary re-locks the task and provider subject, revalidates the latest account, incident, and invited-provider authority, and derives the exact operation-specific provider request hash before fake adapter I/O. Provider-account refresh observations must follow the database-authored prior outcome time, so a stale success cannot supersede a newer restricted or failed fact. The disposable PostgreSQL system journey exercises both `SETTLED` and `FULL_REFUND` terminal paths, including deliberate PREPARED and REQUESTED crash/revocation windows, altered request and reconciliation hashes, a crash after every planned step, and exact replay without duplicate financial facts. These remain local test effects only: they create no hard assignment, real value, approved-provider operation, or production capability. Exact-SHA local and hosted release evidence is recorded separately from this source description; every hosted release check remains pending until a review branch is published.

The current change-order and remaining orchestration boundaries are exact review-candidate claims, not integrated-green claims:

- `UniversalV1ChangeOrderApplication`: Fixture v6 gives `PRICE_AND_SCOPE` a commit-before-I/O sequence. Phase A persists the single-winner change-order witness, fake-only `ADJUST` executes outside the repository transaction, and Phase C revalidates exact authority before materialization.
- Fixture v7 gives unresolved Phase A work a bounded fake-only recovery path. A confirmed no-effect result can terminate without a financial effect; a successful `ADJUST` whose Phase C authority is permanently revoked can use only its exact compensating `REVERSAL`. Neither path restores the prior secured state or resumes execution/capture.
- The v6/v7/v8 fixture bytes and eight-entry bootstrap order are frozen; a complete required gate for the combined dirty source remains pending, and timeout or unknown provider state grants no blind redispatch authority.
- Generic `INGEST_WEBHOOK` is not a canonical causal provider observation. Engine migration 132 instead correlates authenticated observation evidence to an existing command and dispatch attempt, reserves nonterminal/unknown follow-up with expiry, and only corroborates terminal outcomes already recorded.
- Engine migration 133 supplies the canonical append-only double-entry ledger. It derives balanced postings from immutable lifecycle and reconciliation authority, uses `SUSPENSE_UNALLOCATED` for the unexplained residual, and infers no fee or revenue policy.
- Engine migrations 134–138 add observation-only Ops Cases, immutable RelationshipOrigin, held-effect dispute/recovery facts, privacy-safe Task Opportunities/`EXPRESS_INTEREST`, and synthetic completion-notice dispatch; none grants assignment, private-address, live communication, payment, payout, or provider-command authority.
- No approved real-provider adapter exists.
- No production capability policy authorizes any customer-money effect.

Those paths require their own reviewed commit-before-I/O, exact crash/replay, outcome-bridge, and final revalidation proofs. The independently committing `createUniversalV1FakeFinancialApplicationService()` may begin only from already committed lifecycle prerequisites. No web, worker, or production orchestration path may substitute a transaction-bound database adapter or widen the fake-only operation allowlist.

## Production startup

`npm start` and `npm run start:workers` never apply schema changes. Both call the
read-only `runStartupMigrations()` attestor before becoming ready. It hashes the
packaged canonical SQL and performs one `SELECT` against `applied_migrations`;
missing, duplicated, malformed, or drifted evidence fails startup closed.

The only general schema writer is the explicit one-shot migration role:

- `npm run db:migrate:local` is for disposable local/test databases and refuses
  Railway metadata or a production runtime.
- `npm run db:migrate:apply` is the compiled deployment command. Preview and
  staging require `SERVICE_ROLE=migration`, a detached Ed25519 signature made by
  a public key pinned in protected source, a runtime-measured executable digest,
  the exact migration-artifact digest, exact Railway target metadata, and
  `HX_MIGRATION_ENVIRONMENT_APPROVAL_DIGEST` equal to that signed manifest.
- Production execution is additionally held until the exact production Railway
  project ID and an approved release-authority public key are enrolled by a
  protected source change. Runtime variables cannot enroll either trust anchor.

The nonproduction fake-finance bootstrap performs the same authority preflight
before its canonical migration chain opens a database connection. Local test
and migration-certification scripts may continue calling the migration library
against explicitly disposable databases; that library is not an application
startup path. API and worker deployment health now read that bootstrap evidence
without writing schema: a local, preview, or staging runtime is not ready until
one append-only completion row matches the exact release-manifest digest,
migration-artifact digest, release identity, current required-migration count,
and all eight packaged fake-finance SQL checksums in both their schema-evidence
and `applied_migrations` rows. Production reports this nonproduction provider as
disabled. This health attestation grants no payment or migration authority.

## Operator commands

| Command                                                     | Effect                                                                                               |
| ----------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- |
| `npm run db:validate`                                       | Read-only live schema and invariant validation                                                       |
| `npm run db:check`                                          | Alias of the schema validator                                                                        |
| `npm run db:migrate`                                        | Disabled because the old command was destructive                                                     |
| `npm run db:migrate:local`                                  | Explicit migration command for verified disposable local/test databases; refuses Railway             |
| `npm run db:migrate:apply`                                  | One-shot deployed migration role; signed exact-manifest and environment approval required            |
| `npm run db:migrations:checksum-plan -- --manifest <path>`  | Read-only validation of an exact legacy-checksum reconciliation manifest                             |
| `npm run db:migrations:checksum-apply -- --manifest <path>` | Guarded reconciliation of reviewed `NULL` checksum rows; held unless every authority gate is present |
| `npm run db:reset:destructive`                              | Drops and rebuilds `public`; disposable development databases only                                   |

## Legacy checksum reconciliation

Old versions of `applied_migrations` could record a migration name without its SQL checksum. Startup now refuses such rows with `MIGRATION_CHECKSUM_MISSING`; it does not guess that the current local file is the SQL that originally ran. The reconciliation rail is an exceptional evidence-backed repair, not a migration runner and not production authorization.

The checked-in `backend/database/legacy-migration-checksum-reconciliation.HOLD.json` is deliberately empty and non-authorizing. Its schemas are:

- `backend/database/legacy-migration-checksum-reconciliation.schema.json`
- `backend/database/legacy-migration-checksum-signature.schema.json`

An authorized candidate must be stored under `backend/database/migration-checksum-manifests/` and must bind all of the following:

1. The exact reviewed source commit and distinct preparer and reviewer identities.
2. The exact target database, role, server address and port, PostgreSQL version, cluster name, and SHA-256 fingerprint of that canonical identity object.
3. Every row in the target `applied_migrations` ledger, sorted by migration name. Each row names one repository-relative SQL file, its exact local SHA-256, and whether the reviewed ledger value is `NULL_CHECKSUM` or `VERIFIED_CHECKSUM`. Missing, extra, duplicate, drifted, or partially reconciled rows are blockers.
4. The SHA-256 of the canonical payload plus separate Ed25519 review evidence under `backend/database/migration-checksum-signatures/`. The evidence signer must be the named reviewer and must bind the same payload and source commit.

`npm run db:migrations:checksum-plan -- --manifest <path>` only reads target identity, the ledger, and local files. It never starts a transaction, acquires a write lock, or issues an update. A plan that would update zero rows fails as a no-op instead of reporting false success.

Apply additionally requires all of these exact, out-of-band values:

- `NODE_ENV=maintenance`
- `HX_ALLOW_LEGACY_MIGRATION_CHECKSUM_RECONCILIATION=APPLY_EXACT_REVIEWED_MANIFEST`
- `HX_LEGACY_MIGRATION_CHECKSUM_MANIFEST_SHA256=<exact raw manifest SHA-256>`
- `HX_LEGACY_MIGRATION_CHECKSUM_SIGNATURE_SHA256=<exact raw signature-evidence SHA-256>`
- `HX_LEGACY_MIGRATION_CHECKSUM_SIGNER_KEY_SHA256=<exact reviewer public-key SHA-256>`
- for a non-loopback `DATABASE_URL`, `HX_LEGACY_MIGRATION_CHECKSUM_TARGET_FINGERPRINT=<exact observed target fingerprint>`

Apply re-reads the target identity, opens one serializable transaction, takes one transaction-scoped PostgreSQL advisory lock, re-reads the complete ledger, and updates only rows whose checksum remains `NULL`. It then re-reads the complete ledger and commits only when every checksum equals the reviewed local hash. Any failed precondition or update rolls the entire transaction back. It never inserts a ledger row, changes a non-`NULL` checksum, applies SQL, or changes schema.

Do not copy a plan or manifest between targets. Do not infer a checksum from a filename, an equivalent schema, a fresh install, or a successful migration replay. The preparer must assemble evidence from the exact target and exact commit; the independent reviewer must verify and sign it; normal protected-release and environment-approval controls still apply before any persistent-target execution.

## Adding a migration

Persistent-target use of these mechanics requires the Governor dependency gate to authorize an exact migration name and base. An isolated review candidate may complete the same mechanics against disposable local databases, but its registry entry is only a source proposal and grants no authority outside that disposable lane.

1. Add an idempotent SQL file under `backend/database/migrations/`.
2. Add its stable name and filename to `REQUIRED_MIGRATION_FILES` in order.
3. Add tests for the schema contract and repeat-application behavior.
4. Prove that a fresh baseline plus the complete ordered chain and every supported upgrade lineage converge on the same final shape. Fold a final shape into `constitutional-schema.sql` only when the baseline already owns that object and doing so preserves every migration prerequisite. Objects introduced after the baseline remain established by their append-only canonical migrations; do not move them backward across dependencies merely to duplicate their shape.
5. Run schema validation against a disposable PostgreSQL database before production.

Never rewrite or delete an already-applied production migration. Add a repair migration instead.

Never infer a safe path from a filename scan, one database ledger, a provider project list, a secret name, or a local test. Fresh-install success does not prove upgrade safety; upgrade success does not prove replay, drift, rollback/forward-repair, or concurrency safety.
