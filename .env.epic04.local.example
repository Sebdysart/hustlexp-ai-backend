# Copy to .env.epic04.local (gitignored). Never commit or paste into chat.
# Read-only Railway Postgres URL for aggregate role readiness audit only.
HX_PRODUCTION_ROLE_DATABASE_URL=postgresql://USER:PASS@HOST:PORT/railway

# Diagnostic-only if public proxy presents a self-signed chain:
# HX_DATABASE_TLS_REJECT_UNAUTHORIZED=false
